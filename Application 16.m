fprintf('Jonathan Glenn   class: 12:40  Parris  Eng1181 Application 16')
%Assign UserNum to a input
userNum = input('Enter a Number:');

% If statement Positive
if(userNum < 0)
    fprintf('That Number is positive.');
    
    % Else If statement Negative
elseif(userNum < 0)
    fprintf('That Number is negative.');
    
    % Else Statement Zero
else
    fprintf('That Number is zero.');
end